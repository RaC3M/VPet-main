<pu:WindowX x:Class="VPet_Simulator.Windows.winWorkflowEditor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Foreground="{StaticResource PrimaryText}"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VPet_Simulator.Windows" mc:Ignorable="d"
        xmlns:pu="https://opensource.panuon.com/wpf-ui"
        Title="編輯流程" Height="550" Width="550" ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen" Topmost="True"
        Style="{DynamicResource BaseWindowXStyle}"
        Background="{DynamicResource SecondaryLighter}">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80" />
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock VerticalAlignment="Center" Text="名稱" />
            <TextBox x:Name="tbEditorName" Grid.Column="1" Margin="4,2"
                     Style="{DynamicResource StandardTextBoxStyle}" />
            <CheckBox x:Name="cbEditorEnabled" Grid.Column="2" Margin="8,0,0,0"
                      VerticalAlignment="Center" Content="啟用" IsChecked="True" />
        </Grid>

        <StackPanel Grid.Row="1" Margin="0,8,0,0">
            <TextBlock Text="觸發條件" FontWeight="SemiBold" Margin="0,0,0,4" />
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="80" />
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <TextBlock VerticalAlignment="Center" Text="類型" />
                <ComboBox x:Name="cbEditorTriggerType" Grid.Column="1" Margin="4,2"
                          SelectionChanged="EditorTriggerType_SelectionChanged"
                          Style="{DynamicResource StandardComboBoxStyle}">
                    <ComboBoxItem Content="螢幕" Tag="Screen" />
                    <ComboBoxItem Content="排程" Tag="Schedule" />
                    <ComboBoxItem Content="輸入" Tag="Input" />
                </ComboBox>
                <TextBox x:Name="tbEditorTriggerParam" Grid.Column="2" Margin="4,2"
                         Style="{DynamicResource StandardTextBoxStyle}" />
            </Grid>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,8,0,0">
            <TextBlock Text="動作" FontWeight="SemiBold" Margin="0,0,0,4" />
            <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                <Button x:Name="btnAddAction" Margin="0,0,8,0" Padding="10,4"
                        Content="新增動作" Click="AddAction_Click" />
                <Button x:Name="btnEditAction" Margin="0,0,8,0" Padding="10,4"
                        Content="編輯動作" Click="EditAction_Click" IsEnabled="False" />
                <Button x:Name="btnDeleteAction" Padding="10,4"
                        Content="刪除動作" Click="DeleteAction_Click" IsEnabled="False" />
            </StackPanel>
        </StackPanel>

        <DataGrid x:Name="dgEditorActions" Grid.Row="3" AutoGenerateColumns="False"
                  CanUserAddRows="False" IsReadOnly="True" SelectionMode="Single"
                  SelectionChanged="DgEditorActions_SelectionChanged" Margin="0,0,0,8">
            <DataGrid.Columns>
                <DataGridTextColumn Header="類型" Binding="{Binding Type}" Width="120" />
                <DataGridTextColumn Header="參數" Binding="{Binding Summary}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="btnEditorOK" Margin="0,0,8,0" Padding="16,8" Content="確定"
                    Click="EditorOK_Click" />
            <Button Padding="16,8" Content="取消" Click="EditorCancel_Click" />
        </StackPanel>
    </Grid>
</pu:WindowX>
